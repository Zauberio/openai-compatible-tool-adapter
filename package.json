{
  "name": "openai-compatible-tool-adapter",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Generic OpenAI-compatible tool adapter with recipe-based integrations.",
  "bin": {
    "openai-compatible-tool-adapter": "dist/bin/openai-compatible-tool-adapter.js",
    "clawsweeper-codex-adapter": "bin/clawsweeper-codex-adapter.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  },
  "license": "MIT"
}
